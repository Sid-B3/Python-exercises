#These are examples for reading files through code
example1 = "/resources/data/Example1.txt" #Add the location of file on drive
file1 = open(example1, "r") #Use 'r' to read the file, 'w' can be used to write to the file

file1.name   #Prints the path of the file

#Using with statement to read from a file, this does not require us to close the file after reading
with open(example1, "r") as file1:
    FileContent = file1.read()
    print(FileContent)
   
 #Iteration to read through lines of a file
    with open(example1,"r") as file1:
        i = 0;
        for line in file1:
            print("Iteration", str(i), ": ", line)
            i = i + 1;
            
            
 #Printing specific lines of a file
 FileasList[0] 
